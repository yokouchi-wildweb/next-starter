# DOWNSTREAM NOTICE id:20260904-012518-coupon-attribution-program
from: next-starter (Tier1 upstream) | branch:main | commit:f42973b1
date: 2026-09-04T01:25:18Z
severity: action-required
change: Coupon core now rejects self-redemption of attribution coupons (new reason `self_redeem_forbidden`, enforced in validateCouponStatically → isUsable / redeem / validateForCategory) and passes `tx` into handler `onRedeemed`. Two new core domains: couponAttributionReward (per-redemption wallet payout to the coupon issuer, idempotent ledger `coupon_attribution_rewards`) and couponIssuerGrant (apply → approve → periodic self-issue of affiliate coupons, table `coupon_issuer_grants`, program config via src/registry/couponIssuerProgramRegistry.ts, upstream ships null = issue API 503). Reason→message map centralized in coupon/constants/redeemReasonMessages.ts. issueCodeForOwner accepts `settings`; getCodesByOwner accepts `limit`/`order`. completePurchase wraps redeemWithEffect in a SAVEPOINT.
why: schemaRegistry includes two new tables + two new pg enums, so DB drifts from code until pushed. Behavior change: any user who could previously redeem their own invite/affiliate coupon is now rejected (intended fraud-loop fix; official coupons unaffected). If downstream duplicated the coupon reason→message map or extended `UsabilityReason` locally, reconcile with the centralized map.
required_actions:
1. [user-run] pnpm db:push   # creates coupon_attribution_rewards + coupon_issuer_grants and enums coupon_attribution_reward_status / coupon_issuer_grant_status
2. grep -rn "handler_rejected" src --include=*.ts --include=*.tsx — if a downstream file re-declares the coupon reason message map, replace it with getCouponRedeemReasonMessage() from @/features/core/coupon/constants/redeemReasonMessages (exhaustive over UsabilityReason, includes self_redeem_forbidden).
3. If a downstream coupon handler implements onRedeemed and performs DB writes, switch it to use the new `tx` from context (CouponRedeemedContext.tx) so side effects join the caller's transaction.
4. (opt-in) To run an issuer program: define a CouponIssuerProgramConfig and assign it in src/registry/couponIssuerProgramRegistry.ts; register a handler for that category in coupon/handlers/init.ts; to pay issuers on redemption call couponAttributionRewardService.grant(...) from that handler's onRedeemed with context.tx (recipe in src/features/core/couponAttributionReward/README.md).
verify: `pnpm db:tables` lists coupon_attribution_rewards and coupon_issuer_grants; `npx tsc --noEmit` green; POST /api/coupon/check-usability with the issuer's own attribution coupon returns reason self_redeem_forbidden.
manual_steps: -
refs: src/features/core/coupon/README.md (自己消込ガード / onRedeemed tx), src/features/core/couponAttributionReward/README.md, src/features/core/couponIssuerGrant/README.md, src/registry/couponIssuerProgramRegistry.ts, upstream-requests 20260904-095052-coupon-self-redeem-guard / -attribution-reward / -issuer-program
notes: UI is downstream-owned for both new domains (data layer + me/admin routes + hooks only). Notifications are deliberately not built into grant()/review — send them after tx commit (README recipe). /admin/coupons/affiliate placeholder page is unchanged; replace it downstream with a list backed by serviceRegistry `couponIssuerGrant`.
