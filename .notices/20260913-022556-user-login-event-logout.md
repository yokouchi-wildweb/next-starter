# DOWNSTREAM NOTICE id:20260913-022556-user-login-event-logout
from: next-starter (Tier1 upstream) | branch:main | commit:f1546e00
date: 2026-09-13T02:25:56Z
severity: action-required
change: `USER_LOGIN_EVENT_TYPES` gained "logout" (pg enum `user_login_event_type` += 'logout'). POST /api/auth/logout, /pause, /withdraw now record a `user_login_events` row (eventType "logout", ip/ua from ALS, bestEffort) via new `recordLogoutEvent(userId)`. New server-only queries `findSessionHandoffsByUser(userId, opts)` / `findSessionHandoffsByIp(ip, opts)` (userLoginEvent/services/server/sessionHandoff.ts) return "A logged out → B logged in/signed up from the same IP (+UA) within N minutes" rows. New constants SESSION_START_EVENT_TYPES / SESSION_END_EVENT_TYPES / DEFAULT_SESSION_HANDOFF_WINDOW_MINUTES.
why: The enum value is added in Drizzle only; until db:push runs, every logout/pause/withdraw insert fails with `invalid input value for enum user_login_event_type: "logout"` (swallowed by bestEffort → no rows, no visible error). Any downstream code that exhaustively switches on `UserLoginEventType` ("signup" | "login") now receives "logout" and must handle it.
required_actions:
1. grep -rn "eventType\|UserLoginEventType\|USER_LOGIN_EVENT_TYPES" src --include=*.ts --include=*.tsx -l   # find consumers that map event types to labels/badges (e.g. an admin login-history pane). Add a "logout" case (label ログアウト) and make the default branch render the raw value instead of throwing / returning nothing.
2. npx tsc --noEmit   # exhaustive switch / Record<UserLoginEventType, ...> types now require the "logout" key
3. [user-run] pnpm db:push   # ALTER TYPE user_login_event_type ADD VALUE 'logout' (additive, no table rewrite, no downtime)
verify: `pnpm db:query "SELECT unnest(enum_range(NULL::user_login_event_type))"` lists logout; after one POST /api/auth/logout while signed in, `pnpm db:query "SELECT count(*) FROM user_login_events WHERE event_type = 'logout'"` returns >= 1; `npx tsc --noEmit` green.
manual_steps: -
refs: src/features/core/userLoginEvent/constants/index.ts, src/features/core/userLoginEvent/services/server/recordLoginEvent.ts (recordLogoutEvent), src/features/core/userLoginEvent/services/server/sessionHandoff.ts, src/app/api/auth/logout/route.ts, src/app/api/auth/pause/route.ts, src/app/api/auth/withdraw/route.ts, src/features/core/userLoginEvent/README.md (logout semantics + sessionHandoff + UI recipe), upstream-requests 20260913-111715-user-login-event-logout
notes: "logout" = explicit user-initiated session end only (logout / pause / withdraw). Cookie expiry, closed browser and admin-side session invalidation are NOT recorded — never read "no logout row" as "still signed in". pause/withdraw are included so "A withdraws → B signs up on the same device" is detectable (startedBy "signup"). ipAnalytics eventCount now sums start+end events (~2x); distinct-user results unchanged — filter `event_type IN ('login','signup')` if you need start counts only. Demo users are recorded like login; sessionHandoff excludeDemo defaults to true. Removing an enum value later requires enum recreation, so treat the value set as append-only. Session-handoff rows are reference evidence (same Wi-Fi / CGNAT collides on IP; requireSameUserAgent default true narrows it) — combine with fingerprint / answer / behavior axes, do not decide on this alone.
