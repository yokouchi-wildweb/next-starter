# バックエンド構成ドクトリン

- **ステータス**: 採択済み
- **決定日**: 2026-08-03
- **種別**: 意思決定記録（ADR）+ 拘束規律

---

## 決定

現行のバックエンド構成 **Firebase（Auth / Storage）+ Firestore（リアルタイム配信）+ Neon PostgreSQL（主データ、Drizzle）** を維持する。Supabase 等への一本化・乗り換えは行わない。

ただし無方針の現状追認ではなく、後述の「封じ込め規律」を伴う積極的な選択として維持する。

---

## 背景

### 現構成の役割分担

| サービス | 役割 |
|---------|------|
| Firebase Auth | 認証（ソーシャルログイン、メールリンク、JWT） |
| Firebase Storage | メディアストレージ |
| Firestore | リアルタイム配信（chatRoom 等）+ 一部ドメインの CRUD |
| Neon PostgreSQL + Drizzle | 主データ層。audit / analytics / wallet / counting 等の横断基盤はすべてここ |

### 認識している慢性コスト

この構成には、テンプレートとして支払い続けている構造コストが 3 つある。

1. **ユーザーの二重管理** — 真実の源が Firebase Auth（認証）と Postgres の `users`（ロール・ステータス・業務データ）に分裂しており、`getSessionUser` の DB 同期、登録・退会・BAN 時の両側整合という「同期機構そのもの」がコア複雑性になっている
2. **`lib/crud` の二重アダプタ** — Drizzle 版と Firestore 版の 2 実装を維持しており、Firestore 版は機能制限が多い（OR 不可・orderBy 単一・belongsToMany 非対応等）。機能追加のたびに「Firestore 側どうする問題」が発生する
3. **フォーク毎のセットアップ 2 系統** — 下流プロジェクトを 1 つ生むたびに Firebase プロジェクト + Neon プロジェクト + 2 系統の環境変数が必要

---

## 検討した代替案: Supabase への一本化

ゼロから次世代テンプレートを設計する場合の有力案として、Supabase（Postgres + Auth + Storage + Realtime）への一本化を検討した。

### 一本化の利点（認めるもの）

- **ユーザー同期機構の消滅** — 認証ユーザーが自分の Postgres 内（`auth.users`）に住むため、二重管理と同期バグの温床が丸ごと消える
- **CRUD アダプタの単一化** — 第二 DB が不要になり `lib/crud` が Drizzle 一本になる
- **フォーク毎セットアップの 1 系統化**
- **全データ SQL 化** — audit / analytics / DB agent 等の横断機能が例外なく全域に効く
- Postgres + OSS でロックインが浅く、セルフホストへの退避経路がある

### 採用しなかった理由（決定打）

1. **上流テンプレートという立場が認証移行を許さない** — このリポジトリの価値は下流フォーク群との継続的な追随関係にある。「認証プロバイダを乗り換えたので追随せよ」という下流通知は、稼働中フォークの全エンドユーザーアカウントに触る作業を要求することになり、事実上実行不能。バックエンド乗り換えは旧世代テンプレートの EOL 化（エコシステム分断）とセットでしか実行できない
2. **慢性コストは封じ込め可能** — 3 つの構造コストはいずれも実装済み・運用済みの解決策の上に乗っており、「これから広げない」規律で固定費化できる（後述）
3. **残留配当がある** — 留まる限り、SMS 認証・App Check・FCM（プッシュ通知）・モバイル SDK という Firebase エコシステムの強みを追加コストほぼゼロで収穫できる
4. **フォーク経済** — Neon + Firebase の無料枠は寛大で休眠フォークも生き続ける。Supabase 無料枠は 1 週間非アクセスで停止し、本格運用は有料。フォークを多数量産する形態では構造コストになる
5. **ベンダー存続リスクは低い** — Firebase Auth は Google Cloud Identity Platform と同根で整理対象になる位置にない。Neon に何かあっても Postgres + Drizzle である限り接続先変更で逃げられる。ロックインが深いのは Firebase 側の「端」（認証・ストレージ・配信）に限定されている

---

## 拘束規律（封じ込めドクトリン）

維持を選んだ以上、慢性コストが**広がらない**よう以下を拘束ルールとする。

### 1. Firestore の役割は「リアルタイム配信チャネル」に限定する

- 新規ドメインは**原則 Drizzle（`dbEngine: "Neon"`）**
- Firestore を選べるのは、リアルタイム UX（チャット・プレゼンス・ゲーム状態配信等）が**本質要件**であるドメインのみ
- Firestore は「第二のデータベース」ではなく「配信インフラ」と再定義する。業務データ・集計対象データを Firestore に置く拡張は不可

### 2. `lib/crud` の Firestore アダプタは機能凍結する

- 現状の機能制限（`src/lib/crud/README.md` の「Firestore 版との差異」参照）を**仕様として凍結**し、以後の機能拡張は行わない
- Drizzle 側に機能を追加する際、Firestore 側は「無視 or エラー」の対応のみ行い、同等機能の実装は不要

### 3. Firebase 結合面の上限は「認証・ストレージ・配信」の 3 点とする

- この 3 点に留まる限り、将来もし乗り換えが必要になっても手術範囲は変わらない
- FCM・App Check 等の「残留配当」の採用は可（結合面の深化ではなくエコシステム内の収穫）
- 結合面を広げる方向の拡張（例: Firestore への業務データ配置、Firebase Functions へのロジック移設）は不可

### 4. ユーザー同期機構は「コア資産」として扱う

- Firebase Auth ↔ Postgres `users` の同期はこの構成の急所であり、ここのバグだけは致命傷になり得る
- 登録・退会・BAN・ロール変更など同期経路に触れる変更は、必ず両側の整合を検証する（USER_STATUS_HISTORY・audit 配線はこの防衛線の一部）

---

## 再評価トリガー

「ずっと大丈夫か」を常時悩まないため、以下のいずれかが発生した時点でこの決定を再評価する。

1. Firestore または Firebase Auth の**料金体系・提供条件の実質改悪**
2. ユーザー同期起因の障害が**繰り返し**発生し、保守コストが明らかに上振れしたとき
3. **次世代テンプレートを別系統として新設する**事業判断をしたとき（その場合は本書の「検討した代替案」が設計の出発点になる）

---

## 適用範囲

- 本書は Tier1（upstream）およびすべての下流フォークに適用される
- 下流フォークが個別事情でこの規律から逸脱する場合は、自フォークの `.claude/rules/project.md` に逸脱内容と理由を明記すること

## 関連ドキュメント

- `docs/!must-read/設計思想とアーキテクチャ.md` — 全体アーキテクチャ
- `src/lib/crud/README.md` — Firestore アダプタの機能差異（凍結対象の仕様）
- `src/features/README.md` — domain.json の `dbEngine` 選択
