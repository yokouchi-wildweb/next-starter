
# position_fixedが画面ではなく親要素基準になる問題について.md


## 🔍 問題の詳細

CSS Containing Blockの仕様が原因です。

もし親要素に以下のようなスタイルがあると：
```css
.parent {
  transform: translateZ(0);
}
```
このとき、.parent 要素が 新しいコンテインングブロック（containing block）
を作るため、position: fixed な子要素はビューポートではなく
.parent を基準に固定されてしまいます。

---

## 🧪 なぜそうなる？

CSSの仕様で以下のように定められています：

> Fixed positioning is based on the initial containing block unless any ancestor has a transform, filter, perspective, or contain: paint.

この「初期のコンテインングブロック（＝ビューポート）」が、親の transform 等によって変更されるということです。

---

## ⚠️ `position: fixed` に影響する親要素のプロパティ

以下のプロパティが親要素にある場合、**`position: fixed` はビューポートではなくその親要素を基準**とした配置になります。

### 🎯 コンテインングブロックを作る主なプロパティ

| プロパティ            | 備考                                   |
|---------------------|----------------------------------------|
| `transform`         | 最もよくある原因。`translate`, `scale` など |
| `filter`            | `blur`, `grayscale`, `brightness` など |
| `perspective`       | 3D 表現で使われる                      |
| `contain: paint`    | `layout`, `strict` も含む               |
| `will-change`       | `transform`, `filter` を予告しているとき |
| `backdrop-filter`   | 半透明背景のUIなどで利用される         |
| `clip-path`         | 要素をマスクで切り取る時に使う         |
| `mask`, `mask-image`| SVGなどのマスク処理                    |
| `mix-blend-mode`    | 合成モード（乗算など）を使っている場合  |

---

## 🧪 注意点

- `opacity: < 1` は明確に containing block を作りませんが、GPU合成が関係してブラウザの挙動が変わる場合があります。
- `will-change` は開発時のパフォーマンスチューニングでよく使われ、影響を見逃しやすいです。

---

## ✅ 対策方法

1. **不要なプロパティを削除**
2. **`ReactDOM.createPortal` を使って `body` に描画（React限定）**
3. **`absolute` に切り替えて明示的な位置指定**

---

## 🔍 デバッグ方法

Chrome DevTools で該当要素を選択し、右クリック → 「要素のスクロールに固定」などを使って挙動を確認可能。

---
